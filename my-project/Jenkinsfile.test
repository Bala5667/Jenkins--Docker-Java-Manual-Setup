stage('Test') {
    steps {
        script {
            echo "Testing application on http://localhost:${PORT}"

            // Simple curl test
            bat """
                curl --fail http://localhost:${PORT} || exit 1
            """
        }
    }
}
