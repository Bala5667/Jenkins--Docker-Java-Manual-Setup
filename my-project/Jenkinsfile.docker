node {
    stage('Deploy') {
        echo 'Deploying the application...'
        // your deploy commands, like:
        sh 'docker build -t your-image-name .'
        sh 'docker run -d -p 8080:8080 your-image-name'
    }
}
